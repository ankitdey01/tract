// Shared vocabulary. No logic, no local imports.

/** Publish/generation platforms in v1 (YouTube deferred). */
export type Platform = "x" | "linkedin" | "blog";

/** Outcome of the Jev significance gate. */
export type Verdict = "pass" | "fail";
